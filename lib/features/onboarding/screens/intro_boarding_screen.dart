import 'package:flutter/material.dart';

class IntroBoardingScreen extends StatelessWidget {
  const IntroBoardingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
